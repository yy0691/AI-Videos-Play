import React from 'react';

// Per API guidelines, the application must not prompt for an API key.
// This component is obsolete and is replaced with a dummy component to fix compilation errors.
const SettingsModal: React.FC = () => {
  return null;
};

export default SettingsModal;
